# Pelican Version Generator

A simple version generator which generates an incremented version file.

# Usage

```
PLUGINS = [
    'version'
]

VERSION_PATH = PATH + '/version'
OUTPUT_VERSION_PATH = 'output'

```
